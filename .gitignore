node_modules/
.vercel/
public/

# Secrets — NEVER commit processor keys. The verify script reads STRIPE_SECRET_KEY
# from the environment only. .env.example is the committed, value-free template.
.env
.env.*
!.env.example
